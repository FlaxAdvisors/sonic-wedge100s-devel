---
name: feedback_git_commits
description: User manages source control; do not run git commit or git add
type: feedback
---

Do not run `git add` or `git commit` commands. The user manages source control themselves.

**Why:** User preference.

**How to apply:** Never commit files, stage changes, or push branches. If a skill step says "commit the design document," skip that step silently or note it for the user to do manually.
